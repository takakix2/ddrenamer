# DDRenamer (Tauri-Rust Edition) 実装要件定義書

PC破壊により失われた前作の記憶を、Rustの堅牢性とTauriの軽量さで再構築するための実装記録です。

---

## 1. プロジェクト概要

- **コンセプト**: 「迷わない、広い、速い」。タブで機能を選択し、巨大なドロップゾーンに投げ込むだけでリネームを完結させる。
- **コア技術**:
  - **Frontend**: Tauri v2, React (Vite), **Lethe_UI_Kit**, Lucide-react (Icon)
    - ⚠️ **Tailwind CSS は 2026-07-30 に放棄**。Tabula / Alethoglyph と同じデザインシステム
      （`Lethe_UI_Kit`）へ移行済み。`src/ui-kit` は `../../Lethe_UI_Kit` への symlink
  - **Backend**: Rust (std::fs, PathBuf, Regex, same_file)

---

## 2. 実装済み機能ステータス

| 機能 | ステータス | 詳細 |
| :--- | :--- | :--- |
| **定型リネーム** | ✅ 完了 | 拡張子維持/破棄の選択可能 |
| **連番リネーム** | ✅ 完了 | 接頭辞/接尾辞、桁数指定、**元の名前の保持**可能 |
| **手動連番** | ✅ 完了 | `Manual Increment` モードで1ドロップ毎のカウンタ管理 |
| **文字列置換** | ✅ 完了 | **正規表現 (Regex)** による一括置換対応 |
| **追加/削除** | ✅ 完了 | 指定位置（先頭/末尾）への追加および文字数指定削除 |
| **ケース変換** | ✅ 完了 | 大文字/小文字変換。**拡張子保護機能**付き |
| **全角/半角変換** | ✅ 完了 | ASCII ↔ 全角英数の高速マッピング。**拡張子保護機能**付き |
| **拡張子変更** | ✅ 完了 | ドットの有無を問わない安全な拡張子置換 |
| **実行ログ** | ✅ 完了 | 下部へのスライドアニメーション付きログリスト（一意ID管理） |
| **UXフィードバック**| ✅ 完了 | ドラッグオーバー時のビジュアル強調 |
| **Undo** | ✅ 完了 | **バッチ単位**（1ドロップ = 1バッチ）・`Ctrl+Z` / `⌘Z`・ログバーの ↶。履歴はメモリ上のみ（直近20） |
| **i18n / テーマ** | ✅ 完了 | ja / en 切替、テーマ4種（Lethe / Dark / Light / Cyber）。設定モーダル `Ctrl+,` |
| **CSD 窓操作** | ✅ 完了 | 掴み・リサイズ・− □ ✕。⚠️ macOS のみ `decorations: true`（後述） |

---

## 3. システム設計 (Architecture)

### Backend (`src-tauri/src/lib.rs`)
- `Position`, `CaseMode`, `WidthMode` などの **Enum 定義**により、型安全なリネームコマンドを実現。
- `join_name_ext` ヘルパーにより、全てのモードで「ファイル名本体」と「拡張子」を分離して処理。
  **名前の検査はここに集約**（`Result` を返し、空名を stem/ext が出会う 1 点で弾く）。
- 🚨 **上書き防止は「名前の比較」ではなく「実体の同一性判定」**（`same_file`）。
  ⚠️ かつては `new_path.exists()` で判定していたが、**大文字小文字だけ違うファイルを黙って
  上書きする**バグがあり 2026-07-31 に置き換えた。case-insensitive FS では
  `photo.jpg` → `PHOTO.jpg` が「別ファイルが既に在る」と「自分自身」の区別を付けられないため。
  📌 `fs::canonicalize` による比較は採用しなかった（macOS で case-only リネームを塞ぎかねない）。
- **Undo に専用コマンドは無い。** 「戻す」＝元のフルネームへのリネームなので
  `handle_rename` を再利用する。⇒ **同じ番人（同一性判定・上書き拒否）を必ず通る**。
  🚨 独自の戻し処理を書くと、迂回した瞬間にサイレント上書きが復活する。

### Frontend (`src/App.tsx`)
- **Tauri v2 API** (`getCurrentWebview().onDragDropEvent`) を活用したネイティブなイベント処理。
- `configRef` パターンによる、クロージャ内での最新ステートの参照。
- タイムスタンプベースのユニークIDによる安定したログレンダリング。

---

## 4. 今後の拡張 (Pending)

- [ ] **名前の交換 (Swap)**: 2つのファイル名を入れ替えるロジックの追加（一時ファイル経由の3段階リネーム）。
- [ ] **多重拡張子対応**: `.tar.gz` などの正確なパース。
      📌 「拡張子保護」を売りにしている以上、ここは**穴**（`.tar.gz` の `.tar` が stem 扱いになる）。
- [x] **Undo 機能**: 2026-07-31 実装（上記ステータス表・詳細は `HANDOFF.md`）。

### ❌ 取り下げた案

- **サウンド通知（完了時の SE）** —— 2026-08-01 に**削除**。
  結果は既に 1 件 1 行でログに出ており、**通知の層をもう 1 つ増やすことになる**。
  このアプリは「Undo しても掴んだファイルを保持しない」（＝見えない状態を増やさない）
  という判断を採っており、それと同じ基準で落ちる。投げた瞬間に終わる速さも売りなので、
  鳴らす間もない。📌 これは前作から引き継いだ項目で、**本アプリ固有の要求ではなかった**。

---

## 5. 開発者向けメモ

### Linux (Wayland) 環境での制約 — ⚠️ **要再検証**（2026-08-01）
「Wayland ではファイルマネージャーからの D&D が届かない場合がある」と書いてきたが、
**これが実測なのか予防線なのか区別が付かない**。実際には blackcube（Wayland）の
**デスクトップからのドラッグは動いている**ため、D&D 経路そのものは生きている可能性が高い。

🔴 **確かめること**（ソースによって MIME の出し方が変わりうる）:
- ファイルマネージャーから **複数ファイルを一度に**（`text/uri-list` が複数行で来るか）
- **検索結果**から（バラバラの場所のファイルが一度に来る ＝ この道具の一番おいしい使い方）
- **Alethoglyph から**（README が謳っている連携。実測の記録が無い）

📌 **「ドラッグで気軽に投げられる」がこの道具の芯**なので、ここが動くかどうかは
脚注ではなく本体の話。動くなら README の警告文は**利用者を `Ctrl+V` に逃がすだけの損**になる。
なお開発時の検証は `GDK_BACKEND=x11` 経由でしか行っていない（`HANDOFF.md` の「検証の型」）。

---
*Last Updated: 2026-08-01*
*着想の元: soft.NU **Drag&Drop Renamer** — http://nu.way-nifty.com/top/dragdrop_renamer/index.html*
*⚠️ 移植でも後継でもない（名前も異なる）。実装・UI・機能は独自。*

📌 **現在の状態・未了の follow-up は `HANDOFF.md` が持つ。** この文書は設計の記録で、
日々の進捗を追う場所ではない（同じ事実を両方に書くと片方が必ず腐る）。
